import {StyleSheet} from "react-native";
import {LIGHT_COLOR} from "styles/constants";

const screenStyles = StyleSheet.create({
    defaultStyle: {
        backgroundColor: LIGHT_COLOR,
        flex: 1,
        flexDirection: "column"
    }
});

export default screenStyles;
